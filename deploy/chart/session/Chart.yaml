apiVersion: v2
name: session
description: >-
  session.fasttrackstudio.app — the Session site: the landing page and
  the live in-browser setlist demo. A static wasm bundle and nothing
  else; there is no server and no state — the demo's "backend" is an
  in-process daw-standalone instance running inside the visitor's tab.
type: application
home: https://github.com/FastTrackStudios/session
sources:
  - https://github.com/FastTrackStudios/session
# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the workspace version ([workspace.package].version). The image
# tag defaults to this unless values override it, which in practice they
# always do — argocd-image-updater writes the digest it rolled.
appVersion: "0.1.0"
