[package]
name = "keyflow-chordpro"
version.workspace = true
edition = "2021"  # original edition of the dissolved workspace (root is 2024)
authors.workspace = true
license.workspace = true
description = "ChordPro 6.07 parser and AST. The lyric+chord side of keyflow charts."

[features]
default = []
serde = ["dep:serde"]

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
