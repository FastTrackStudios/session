[package]
name = "keyflow-text"
version.workspace = true
edition = "2021"  # original edition of the dissolved workspace (root is 2024)
license.workspace = true
description = "Keyflow text parsing and highlighting helpers"

[features]
default = []
highlighting = ["keyflow-syntax/highlighting"]

[dependencies]
keyflow-proto.workspace = true
keyflow-syntax.workspace = true
keyflow-chordpro.workspace = true
tracing = { workspace = true }

[lints]
workspace = true
