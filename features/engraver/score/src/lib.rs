//! Score engraving — orchestral scores + extracted parts.
//!
//! Placeholder: the score-engraving spec landed on main referencing
//! this workspace member before the crate itself was committed (which
//! broke every `cargo` invocation tree-wide). This stub restores the
//! build; the implementation follows the spec doc.
