[package]
name = "chord-tool-daw"
version.workspace = true
edition.workspace = true
description = "chord-tool-daw — writes chord-tool's chords into a DAW project"

[dependencies]
# The GUI's sink trait. chord-tool itself stays DAW-free so its panel can
# run standalone; this crate is the half that knows about projects.
chord-tool = { workspace = true }
daw = { workspace = true }
daw-proto = { workspace = true }
tracing = { workspace = true }

[lints]
workspace = true
